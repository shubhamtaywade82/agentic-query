# @agentic-query/prisma

Prisma adapter foundation for Agentic Query.

The adapter translates the language-neutral Query AST into constrained Prisma arguments. It does not accept model names, field expressions, or executable code from the LLM without validation by the Agentic Query runtime.
