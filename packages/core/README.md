# @agentic-query/core

Core query contracts and deterministic policy validation for Agentic Query. This package intentionally does not execute database queries; adapters translate validated queries into native ORM/database operations.
