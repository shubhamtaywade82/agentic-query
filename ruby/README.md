# agentic_query

Ruby implementation of the Agentic Query runtime. The initial release provides the gem namespace and deterministic query validation. Rails/ActiveRecord integration will be added as a dedicated adapter after the shared query contract is stabilized.
