# Agentic Query Specification

The `spec/` directory defines the language-neutral contract shared by Ruby and TypeScript implementations.

The LLM produces data conforming to this specification. Trusted runtime code validates, authorizes, compiles, and executes it.
